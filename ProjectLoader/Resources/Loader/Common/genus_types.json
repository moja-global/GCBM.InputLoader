{
    "SQLLoaderMapping": {
        "name": "Genus types",
        "fetch_sql": "
            SELECT DISTINCT name AS GenusName
            FROM genus g
            INNER JOIN genus_tr g_tr
                ON g.id = g_tr.genus_id
            INNER JOIN locale
                ON g_tr.locale_id = locale.id
            WHERE locale.code = 'en-CA'",
        "load_sql": "INSERT INTO genus (name) VALUES (@GenusName)"
    }
}