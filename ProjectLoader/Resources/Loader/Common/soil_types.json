{
    "SQLLoaderMapping": {
        "name": "Soil types",
        "fetch_sql": "
            SELECT DISTINCT name AS SoilType
            FROM afforestation_pre_type_tr soil
            INNER JOIN locale
                ON soil.locale_id = locale.id
            WHERE locale.code = 'en-CA'",
        "load_sql": "
            INSERT INTO soil_type (name)
            VALUES (@SoilType)"
    }
}
