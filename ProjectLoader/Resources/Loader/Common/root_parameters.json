{
    "SQLLoaderMapping": {
        "name": "Root parameters",
        "fetch_sql": "
            SELECT * FROM root_parameter",
        "load_sql": "
            INSERT INTO root_parameter (hw_a, sw_a, hw_b, frp_a, frp_b, frp_c)
            VALUES (@hw_a, @sw_a, @hw_b, @frp_a, @frp_b, @frp_c)"
    }
}